// Ext lib
import { Title } from "@telegram-apps/telegram-ui";
import React from "react";


const ModalMainPageTitle = ({}) => {
    return (
        <Title level="2" weight="1" plain={false} style={{padding: 0}}>
            Мои адреса
        </Title>
    )
}

export default ModalMainPageTitle;